---
id: 01M0H6M3SJSP19Q309N32GQH4S
slug: HS2-6H36DV
title: Codex app-server drive (AppServer transport + BackingService) — persistent, no new instance per play
category: feature
priority: high
status: completed
created_at: 2026-08-20T22:25:34.201Z
updated_at: 2026-08-20T22:41:23.574Z
completed_at: 2026-08-20T22:41:23.574Z
closed_at: 2026-08-20T22:41:23.574Z
close_reason: completed
legacy_number: HS2-110
schema: 1
---

Build the AppServer transport drive in hotsheet-aitools so Codex is driven the HS1 way — a turn on the running app-server daemon, NOT a fresh process per play (corrects the wrong spawn default). Codex 0.148 exposes: `codex app-server daemon` (JSON-RPC over a control socket), `codex app-server proxy` (proxy stdio to the running daemon), `remote-control start/stop/pair`, and `codex exec resume <id> <prompt>` (no-daemon session resume).

Approach (testable, mirrors the spawn drive's injected-adapter pattern):
- Research the protocol first: `codex app-server generate-json-schema` / `generate-ts` to get method names + the turn flow (newConversation/resume, sendUserTurn/turn-start, turn events, stopReason).
- Ports: an injected AppServerClient (send request / receive turn events) + a BackingService (is-daemon-up / prestart / ensureUp) — docs/13 §13.5.
- AppServerDrive impl Drive: run() ensures the service, starts/resumes a conversation, sends the turn, returns a TurnHandle over the event stream (busy→done from turn events/stopReason).
- Real transport: spawn `codex app-server proxy` (stdio JSON-RPC) or connect the control socket; ensure the daemon via app-server daemon/remote-control. Fake transport in tests scripts requests/responses — no real daemon.

Also: flip the Codex plugin [drive] to transport = app-server (keep exec-resume spawn as a documented fallback).

Deferred if large: the full live daemon connection can be a follow-up; this ticket lands the drive logic + ports + fake-tested turn flow + manifest/doc correction.

Relates to: HS2-106/108, docs/13 §13.5, HS2-9 (Claude channel), docs/121 (HS1 app-server).

## Notes

<!-- note: 01M0H6M3T80WDFZCB70XQYDA95 -->
2026-08-20T22:41:23.574Z — Done (drive logic) + verified. Researched the codex 0.148 app-server protocol (generate-ts): thread/start|resume, turn/start|steer|interrupt, turn/started→turn/completed notifications, approval ServerRequests. Built AppServerDrive over an injected AppServerClient/AppServerTurn port (fake daemon in tests): opens/resumes a thread (Target selects which), runs a turn with NO process spawned, interrupt via turn/interrupt, not-connected + daemon-down errors. Codex plugin [drive] flipped spawn→app-server; SpawnDrive now for agy/codex-exec fallback. docs/13 §13.0 gained a verified current-tool drivability matrix; docs/05 §5.5 + CODEBASE-MAP updated. 122 tests pass; committed 7e34d44 (pushed).

Follow-up filed HS2-112: the LIVE daemon connection (real AppServerClient over `codex app-server proxy` JSON-RPC) — deferred as planned.
